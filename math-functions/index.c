#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    float x = 3;
    // x = sqrt(x);
    // x = round(x);
    // x = ceil(x);
    // x = floor(x);
    // x = log(x);
    // x = sin(x);

    printf("%f", x);
    return 0;
}